## Changelog

### UI system + page polish

- Added a reusable UI system (CSS variables, cards, buttons, badges, icon buttons, toggles, grids).
- Refreshed Dashboard / Safety / Earnings / Trips / Settings to use the shared components and theme.
- Added accessible focus rings, hover micro-interactions, and responsive grid behavior.
- Stored Settings (goals/notifications/appearance/account) in `localStorage`.
- Confirmed production build runs clean via `npm run build`.

